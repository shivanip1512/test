<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.cannontech.web.api">
        
        <!-- As APIs are added into their own files referenced in RestApiWebApplicationInitializer, we can exclude them here. -->
        
        <!-- Handled in WEB-INF/contexts/api/api-context-ecobee.xml -->
        <context:exclude-filter type="regex" expression="com.cannontech.web.api.dr.ecobee.*"/>
        
        <!-- Handled in WEB-INF/contexts/api/api-context-aggregateIntervalDataReport.xml -->
        <context:exclude-filter type="regex" expression="com.cannontech.web.api.aggregateIntervalDataReport.*"/>
        
    </context:component-scan>
</beans>
