package com.cannontech.core.authorization.support.string;

import com.cannontech.core.authorization.support.Authorization;

/**
 * Interface for authorizing user permissions based on a string representing an
 * object.
 */
public interface StringAuthorization extends Authorization<String> {
}