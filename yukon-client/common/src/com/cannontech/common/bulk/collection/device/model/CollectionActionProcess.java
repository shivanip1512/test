package com.cannontech.common.bulk.collection.device.model;

public enum CollectionActionProcess {
    //inserts data in database - Add Points
    DB,
    //sent to porter - Read Attribute
    CRE
}
