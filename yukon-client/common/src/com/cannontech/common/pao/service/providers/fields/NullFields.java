package com.cannontech.common.pao.service.providers.fields;

import com.cannontech.common.pao.service.PaoTemplatePart;

public class NullFields implements PaoTemplatePart {
    //We can't return null class types in the providers, so this blank Fields object will represent the null case.
}
