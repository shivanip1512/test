package com.cannontech.common.rfn.message.alarm;

public enum AlarmCategory {
    GW_ALARM,
    NM_ALARM,
    NM_ALARM2,
    ; // may extend to Node_ALARM, SENSOR_ALARM, etc., in the future
}
