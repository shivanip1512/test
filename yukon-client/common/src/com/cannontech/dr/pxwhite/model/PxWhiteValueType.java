package com.cannontech.dr.pxwhite.model;

/**
 * API documentation lists BOOL, INT, FLOAT, STRING, TIME, BINARY, ENUM.
 * Types seen in the wild include INT32, UINT32, DOUBLE.
 */
public enum PxWhiteValueType {
    BOOL,
    INT,
    INT32,
    UINT32,
    FLOAT,
    DOUBLE,
    STRING,
    TIME,
    BINARY,
    ENUM,
    ;
}
